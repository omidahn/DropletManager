<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".DropletManagerApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DropletManager"
        android:usesCleartextTraffic="false"
        tools:targetApi="31">

        <activity
            android:name=".ui.activity.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activity.MainActivity"
            android:exported="false" />

        <activity
            android:name=".ui.activity.SshConsoleActivity"
            android:exported="false"
            android:label="SSH Console" />

        <activity
            android:name=".ui.activity.CpuMetricsActivity"
            android:exported="false"
            android:label="CPU Usage" />

        <activity
            android:name=".ui.activity.MemoryMetricsActivity"
            android:exported="false"
            android:label="Memory Usage" />

        <activity
            android:name=".ui.activity.UsageMetricsActivity"
            android:exported="false"
            android:label="Usage" />

        <activity
            android:name=".ui.activity.SshSettingsActivity"
            android:exported="false"
            android:label="SSH Console Settings" />

        

        <!-- Removed legacy activities that were deleted during Compose migration -->

    </application>

</manifest>
